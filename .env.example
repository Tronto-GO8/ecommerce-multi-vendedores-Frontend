# ⚠️ ATENÇÃO: Este arquivo é um modelo para um arquivo que deve ser criado em sua máquina local que contém informações sensíveis!
# Cada participante deve criar seu próprio arquivo .env no raiz do repositório copiando este exemplo
# e preencher com os valores reais (senhas, chaves, tokens, URLs de banco, etc.) que seram fornecidos no privado.
# Nunca compartilhe este arquivo ou comite no GitHub, mas casso aconteça o .gitgnore já foi setado para ignorar o arquivo .env.

# -------------------------------
# Configurações do backend
# -------------------------------

# Porta que o backend vai rodar
PORT=3000
# -------------------------------
# Banco de dados
# -------------------------------

# String de conexão (ex: PostgreSQL)
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DATABASE

# -------------------------------
# Email (SMTP) - Nodemailer
# -------------------------------
EMAIL_USER=seuemail@gmail.com
EMAIL_PASS=sua_senha_app

# -------------------------------
# OAuth2 (Google)
# -------------------------------
GOOGLE_CLIENT_ID=seu_client_id_aqui
GOOGLE_CLIENT_SECRET=seu_client_secret_aqui
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# -------------------------------
# JWT / autenticação interna
# -------------------------------
JWT_SECRET=chave_super_secreta_para_tokens
JWT_EXPIRES_IN=1h

# -------------------------------
# Outras chaves/API de serviços
# -------------------------------
